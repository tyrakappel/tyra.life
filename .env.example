# --- Database ---
# Vercel Postgres / Neon / Supabase Postgres connection string
DATABASE_URL="postgresql://user:password@localhost:5432/tyra_life?schema=public"

# --- NextAuth (Auth.js v5) ---
# Generate: openssl rand -base64 32
AUTH_SECRET=""
AUTH_URL="http://localhost:3000"

# Email magic link (Resend). Skip if using Google only.
AUTH_RESEND_KEY=""
EMAIL_FROM="Tyra Life <noreply@tyra.life>"

# Google OAuth (optional)
AUTH_GOOGLE_ID=""
AUTH_GOOGLE_SECRET=""
